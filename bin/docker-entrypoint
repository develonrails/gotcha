#!/bin/bash -e

# Create or migrate every configured database (primary, cache, queue) on
# every container start. db:prepare iterates all DB configs from
# database.yml and runs pending migrations from each one's migrations_paths.
./bin/rails db:prepare

exec "${@}"
